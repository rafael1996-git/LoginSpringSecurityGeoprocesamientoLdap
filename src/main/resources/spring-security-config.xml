<beans:beans
	xmlns="http://www.springframework.org/schema/security"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:p="http://www.springframework.org/schema/p" 
	xmlns:beans="http://www.springframework.org/schema/beans"
	xsi:schemaLocation="http://www.springframework.org/schema/security
                                 http://www.springframework.org/schema/security/spring-security.xsd
                                 http://www.springframework.org/schema/beans
                                 http://www.springframework.org/schema/beans/spring-beans.xsd
                                 http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">
	<context:component-scan
		base-package="com.ine.cartografia.*" />
	<http auto-config="true" use-expressions="true">
		<intercept-url pattern="/admin"
			access="hasRole('MANAGERS') or hasRole('DEVELOPERS') or hasRole('SUBMANAGERS')" />
		<intercept-url pattern="/agregar"
			access="hasRole('MANAGERS')" />
		<intercept-url pattern="/delete"
			access="hasRole('MANAGERS')" />
		<intercept-url pattern="/viewAdmin"
			access="hasRole('MANAGERS') or hasRole('DEVELOPERS') or hasRole('SUBMANAGERS')" />
		<intercept-url pattern="/avance"
			access="hasRole('MANAGERS') or hasRole('SUBMANAGERS')" />
		<intercept-url pattern="/MultiProcesos"
			access="hasRole('MANAGERS') or hasRole('DEVELOPERS')" />
		<intercept-url pattern="/register"
			access="hasRole('MANAGERS') " />
		<intercept-url pattern="/welcome"
			access="hasRole('MANAGERS') or hasRole('SUBMANAGERS')" />
		 <intercept-url pattern="/css/**" access="permitAll" />     
 		 <intercept-url pattern="/js/**" access="permitAll" />
 		 <intercept-url pattern="/images/**" access="permitAll" />
 		 <intercept-url pattern="/stilosjs/css/**" access="permitAll" />
 		  <intercept-url pattern="/stilosjs/**" access="permitAll" />
 		   <intercept-url pattern="/core/css/**" access="permitAll" />
 		    <intercept-url pattern="/core/js/**" access="permitAll" />
 		 <intercept-url pattern="/funcion/**" access="permitAll" />
 		 <intercept-url pattern="/static/**" access="permitAll" />

		<form-login login-page="/login"
			login-processing-url="/appLogin" username-parameter="username"
			password-parameter="password" default-target-url="/admin"
			authentication-failure-url="/login?error" />

		<logout logout-url="/logout" logout-success-url="/?logout" />
		<csrf />
		<access-denied-handler error-page="/Error" />
		<anonymous />
	</http>

	<beans:bean name="passwordEncoder"
		class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder" />

	<authentication-manager>
		<ldap-authentication-provider
			server-ref="ldapServer" user-dn-pattern="uid={0},ou=people"
			user-search-base="ou=people" user-search-filter="uid={0}"
			group-search-base="ou=groups" group-search-filter="uniqueMember={0}"
			role-prefix="ROLE_">
			<password-compare password-attribute="userPassword">
				<password-encoder ref="passwordEncoder" />
			</password-compare>
		</ldap-authentication-provider>
	</authentication-manager>

	<ldap-server id="ldapServer"
		url="ldap://localhost:8389/dc=springframework,dc=org" />
	<beans:bean id="accessDeniedHandler"
		class="com.ine.cartografia.filter.CustomAccessDeniedHandler">
		<beans:property name="accessDeniedUrl"
			value="/AdministracionDelGeoprocesamiento/admin" />
	</beans:bean>

	<beans:bean id="customUserDetailsService"
		class="com.ine.cartografia.filter.CustomUserDetailsService" />
	
</beans:beans>